
/***
 * SOURCE_IMPORTS FUNCTION
 *
 * Function used to assist in loading all the modules that will be used for the
 * Visual Quality Assistance Console Project. This returns a key value pair with
 * the key being any name, although making it the same name as the module being
 * loaded would ease the effort of debugging.
 *
 * example:
 * {
 *   <name-of-module>: <function-reference>
 * }
 *
 * @function
 * @return {Object} Key-value pair of modules to load
 * */
export default (function SOURCE_IMPORTS () {
  
  return {

  };

})();
